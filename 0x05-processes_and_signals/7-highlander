#!/usr/bin/env bash
# A Bash script that prints "To infinity and beyond" indefinitely every 2 seconds
# and prints "I am invincible!!!" then "OK" when it receives SIGTERM signal and continues.

trap 'echo "I am invincible!!!"; echo "OK"' SIGTERM

while true; do
    echo "To infinity and beyond"
    sle
